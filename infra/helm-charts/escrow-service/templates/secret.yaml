apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.env.WALLET_SECRET_NAME }}
type: Opaque
stringData:
  # In CD we won't commit secrets — ArgoCD should be configured to patch/create this secret from a vault
  # Here we leave a placeholder to avoid Helm failing; in production populate from CI/CD secrets manager
  {{ .Values.env.WALLET_SECRET_KEY }}: "{}"
