apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.env.WALLET_SECRET_NAME }}
type: Opaque
stringData:
  {{ .Values.env.WALLET_SECRET_KEY }}: |-
{{ .Files.Get "files/escrow-fee-payer.json" | indent 4 }}
