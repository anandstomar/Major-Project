apiVersion: v1
kind: Service
metadata:
  name: analytics-api-service
  namespace: default
spec:
  selector:
    sparkoperator.k8s.io/app-name: analytics-consumer
    spark-role: driver
  ports:
    - name: flask-api
      port: 5001
      targetPort: 5001
      protocol: TCP