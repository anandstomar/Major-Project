apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: query-service
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    # CORRECTION HERE: major-project (Hyphen, not underscore)
    repoURL: https://github.com/anandstomar/major-project.git
    targetRevision: main
    
    # We also keep the "Direct Path" fix from before:
    path: infra/helm-charts/query-service
    helm:
      valueFiles:
      - ../../argocd-apps/query-service/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
