apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: anchoring-scheduler
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    # CORRECTION HERE: Major-Project (Hyphen, not underscore)
    repoURL: https://github.com/anandstomar/Major-Project.git
    targetRevision: main
    
    # We also keep the "Direct Path" fix from before:
    path: infra/helm-charts/anchoring-scheduler
    helm:
      valueFiles:
      - ../../argocd-apps/anchoring-scheduler/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
